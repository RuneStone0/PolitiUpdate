[tool]
line-length = 100

[tool.coverage.run]
omit = ["src/bot/auth.py"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-v --tb=short --cov=src/bot --cov-report=term-missing --cov-fail-under=95"
